#include "./queue.h"

int main(int argc, char const *argv[]) {
  Queue queue;
  init(&stack);
  enque(&stack, 0);
  enque(&stack, 50);
  euque(&stack, 100);
  // 在系统中运行命令：
  // 初始化、入栈、出栈、栈顶
  return 0;
};
